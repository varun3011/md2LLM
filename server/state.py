jobs: dict[str, dict] = {}

# Training jobs — separate from generation jobs
training_jobs: dict[str, dict] = {}

# Dependency install jobs — separate from training execution jobs
install_jobs: dict[str, dict] = {}
